import './AddButton.css'
import {CardButton} from "../CardButton/CardButton";

export const AddButton = () => {
    return (
        <CardButton>
            Новое воспоминание
        </CardButton>
    );
};